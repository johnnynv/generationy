importcsv <- function(){
	sdata <- read.csv("trans.csv")
	return(sdata)
}

calc_mean_nep <- function(nep){
	if(1=length(nep){
		if("integer"=typeof(nep)){
			nstr<-paste("NEP",nep,sep="")
			return(mean(sdata[[nstr]]))
		} else if
	}
}

getwd()
